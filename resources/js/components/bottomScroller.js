document.addEventListener("DOMContentLoaded", function () {
  var chatContainer = document.querySelector(".chat-container")
  chatContainer.scrollTop = chatContainer.scrollHeight // Scroll to the bottom on page load
})
